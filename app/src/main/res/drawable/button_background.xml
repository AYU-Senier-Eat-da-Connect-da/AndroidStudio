<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 버튼의 배경색을 투명으로 설정 -->
    <solid android:color="@android:color/transparent" />

    <!-- 테두리 설정 -->
    <stroke
        android:width="2dp"
        android:color="#E6E6E6" /> <!-- 테두리 색상 (초록색) -->

    <!-- 테두리 모서리를 둥글게 설정 -->
    <corners android:radius="8dp" />
</shape>